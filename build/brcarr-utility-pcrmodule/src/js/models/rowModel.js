var define, console;
define(['framework-core'], function (Core) {
    'use strict';
    
    return Core.Model.extend({
       
        /*initialize: function () {
            // Read config.json to get pcrModule settings
            var pcrModuleSettings = Core.App.config.pcrSettings || {};
            console.log(pcrModuleSettings);
        }*/
    });
});